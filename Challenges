class Obstacle:
    def __init__(self, x, y, w, h):
        self.rect = pygame.Rect(x, y, w, h)
    def draw(self):
        pygame.draw.rect(screen, (150,150,150), self.rect)
obstacles = [Obstacle(100,200,50,50), Obstacle(300,350,80,20)]
